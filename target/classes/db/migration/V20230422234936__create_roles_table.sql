create table roles (
         id int8 generated by default as identity,
         name varchar(255) not null,
         created_at timestamp,
         updated_at timestamp,
         primary key (id)
);

insert into roles (name, created_at, updated_at) values ('seller', now(), now()), ('buyer', now(), now());